server:
  port: 7010
spring:
  application:
    name: lab-user
  datasource:
    master:
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: DbPassword
      jdbc-url: jdbc:mysql://www.seven-io.top:6306/laboratory?characterEncoding=utf-8
    slave:
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: DbPassword
      jdbc-url: jdbc:mysql://www.seven-io.top:6307/laboratory?characterEncoding=utf-8   # 这个地方必须写成jdbc-url
  redis:
    host: www.seven-io.top
    port: 6379
    password: RedisPassword
    database: 0
eureka:
  instance:
    hostname: localhost
    instance-id: UserModule
    prefer-ip-address: true
  client:
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:7000/eureka